// Deployment configuration.
//
// Local development uses the default below. When deploying the frontend
// (Netlify, Vercel, GitHub Pages), set the URL of your deployed backend here —
// this is the only file that needs to change.
export const API_BASE_URL =
    window.API_BASE_URL ?? "http://localhost:8000";
